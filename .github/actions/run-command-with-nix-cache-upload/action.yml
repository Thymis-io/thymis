name: "Run command with Nix cache upload"
inputs:
  script:
    description: "The script to run"
    required: true
  working-directory:
    description: "The working directory to run the script in"
    required: false
    default: "."
  exclude_result:
    description: "If 'true', exclude the ./result store path (and its outputs) from the cache upload. Useful for large build outputs like SD images."
    required: false
    default: "false"
  aws_access_key_id:
    description: "Hetzner S3 access key ID for cache upload."
    required: false
    default: ""
  aws_secret_access_key:
    description: "Hetzner S3 secret access key for cache upload."
    required: false
    default: ""
  signing_key:
    description: "Nix store signing key (cache.thymis.io-1:...) for cache upload."
    required: false
    default: ""
  github_token:
    description: "GitHub token (unused, accepted for compatibility)."
    required: false
    default: ""
  timeout_minutes:
    description: "Maximum minutes to allow the script to run before timing out."
    required: false
    default: "60"
runs:
  using: "composite"
  steps:
    - name: Snapshot store before build
      if: ${{ inputs.signing_key != '' }}
      shell: bash
      run: |
        nix path-info --all 2>/dev/null | sort > /tmp/nix-store-before.txt
    - name: Run script
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        THYMIS_SCRIPT: ${{ inputs.script }}
      run: |
        _SCRIPT_FILE=$(mktemp)
        trap 'rm -f "$_SCRIPT_FILE"' EXIT
        printf '%s\n' "$THYMIS_SCRIPT" > "$_SCRIPT_FILE"
        timeout ${{ inputs.timeout_minutes }}m bash "$_SCRIPT_FILE"
    - if: ${{ inputs.signing_key != '' }}
      name: Upload new store paths to Nix cache
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        AWS_ACCESS_KEY_ID: ${{ inputs.aws_access_key_id }}
        AWS_SECRET_ACCESS_KEY: ${{ inputs.aws_secret_access_key }}
      run: |
        KEY_FILE=$(mktemp)
        trap 'rm -f "$KEY_FILE"' EXIT
        echo "${{ inputs.signing_key }}" > "$KEY_FILE"

        # Find all store paths added during this job
        nix path-info --all 2>/dev/null | sort > /tmp/nix-store-after.txt
        NEW_PATHS=$(comm -13 /tmp/nix-store-before.txt /tmp/nix-store-after.txt)

        # Filter: exclude result symlink target (if requested), known-bad patterns,
        # and paths over 1 GB. Everything else goes straight to nix copy — it
        # checks the destination narinfo before uploading, so already-cached paths
        # are skipped automatically without us having to query upstream caches first.
        MAX_SIZE=$((1024 * 1024 * 1024))
        _CAND=$(mktemp)
        trap 'rm -f "$KEY_FILE" "$_CAND"' EXIT

        EXCL_RESULT=''
        if [ "${{ inputs.exclude_result }}" = "true" ] && [ -L ./result ]; then
          EXCL_RESULT=$(readlink -f ./result)
        fi

        echo "$NEW_PATHS" | while IFS= read -r path; do
          [ -z "$path" ] && continue
          [ -n "$EXCL_RESULT" ] && [ "$path" = "$EXCL_RESULT" ] && continue
          echo "$path" | grep -qE '(fill-image-with-secrets|\.img$|nixos-vm)' && continue
          size=$(du -sb "$path" 2>/dev/null | awk '{print $1}')
          if [ -z "$size" ] || [ "$size" -ge "$MAX_SIZE" ]; then
            echo "Skipping $path (too large: ${size:-unknown} bytes)" >&2
            continue
          fi
          echo "$path" >> "$_CAND"
        done

        if [ ! -s "$_CAND" ]; then
          echo "No new store paths to upload."
          exit 0
        fi

        echo "Uploading $(wc -l < "$_CAND") new store paths to cache..."
        # Cache upload is best-effort: transient S3 errors must not fail the job.
        # nix copy checks the destination narinfo before uploading each path, so
        # already-cached paths (e.g. from cache.nixos.org mirrors) are skipped
        # automatically. Run 4 parallel copy workers to saturate the link.
        xargs -a "$_CAND" -P 4 -n 64 nix copy \
          --to "s3://thymis-nix-cache?endpoint=nix-cache-s3.thymis.io&region=garage&secret-key=${KEY_FILE}" \
          --no-check-sigs \
          || echo "Warning: nix copy exited $? — transient S3 error likely; some paths may not have been cached. Non-fatal."
