# Optional Thymis Assistant configuration.
# A model configured through OpenRouter with tool and image-input support.
THYMIS_AGENT_MODEL=openrouter:openai/gpt-4.1-mini

# Export this in the shell that starts the controller; never commit API keys.
# export OPENROUTER_API_KEY='…'
